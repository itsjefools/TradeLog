# 実装ログ — 指示_21〜_28 (2026-05-23)

Cowork指示書 `指示_21`〜`指示_28` を番号順に実装。対応言語は ja/en/pt/es の4言語（中国語は対象外）。

## ⚠️ 最優先: SupabaseでSQLを2本実行してください

これまで通り、Supabaseダッシュボードの **SQL Editor** に貼り付けて実行してください。コード側は実行済み前提で動きます。

1. `supabase/migrations/0035_app_feedback.sql` — アプリ内フィードバック保存用テーブル `app_feedback`（指示_25）。**未実行だとフィードバック送信が失敗します。**
2. `supabase/migrations/0036_performance_indexes.sql` — 検索高速化のインデックス（指示_26）。任意だが推奨。なくても動作はする。

---

## 各指示の実装内容

### 指示_21 プレミアムUI改善 ✅（SQL不要）
- `lib/premium-features.ts` 新規（特典6項目を型付き定義）。`lib/premium.ts` の旧文字列配列は削除
- `components/premium-gate.tsx` 新規（非プレミアムにロック画面を出す共通コンポーネント）
- ペイウォール `app/school/premium.tsx` を6特典リスト表示に刷新 + 年額に「34%お得」バッジ
- `app/trade-stats.tsx` を `PremiumGate` で保護（統計は有料機能）
- PROバッジは既存 `PremiumBadge`（アバターのダイヤ）を流用
- 翻訳キー `premium.*` を16件追加（4言語）

### 指示_22 空状態とスプラッシュ ✅（SQL不要）
- `components/empty-state.tsx` / `components/loading-view.tsx` 新規（共通コンポーネント）
- フィード / 通知 / 取引履歴 / 統計 / 壁紙ギャラリー の空状態を共通化。フィード・取引履歴にはアクションボタン付き
- `app.json` のスプラッシュ背景を黒(#000000)に統一
- 翻訳キー `empty.*` を4言語追加

### 指示_23 UI全体ポリッシュ ✅（SQL不要）
- `lib/design.ts` 新規（spacing / borderRadius / fontSize / shadow / ACTIVE_OPACITY トークン）
- `lib/haptics.ts` に指示準拠の命名関数を追加（lightImpact / mediumImpact / successNotification / errorNotification / selectionFeedback）
- `components/button.tsx` 新規（primary/secondary/ghost/danger × sm/md/lg、Haptics内蔵）
- 新規画面（統計・エクスポート）の期間フィルタ等にHaptics適用
- ※ 全画面のフォーム/セパレーター一括統一は、就寝中の事故防止のため見送り（既存も概ね統一済み）

### 指示_24 Firebase Analytics ⚠️（アプリ側実装済み・ネイティブ設定は要対応）
- `lib/analytics.ts` 新規（trackScreen / trackEvent / setUserProperties / setAnalyticsUserId / AnalyticsEvents）。**現状はno-op（開発時のみconsole出力）**
- 計測呼び出しを各所に配置済み: 画面遷移の自動追跡(`_layout`)、サインアップ/ログイン、取引記録、投稿作成、ペイウォール表示、購入開始、オンボーディング完了/スキップ、CSVエクスポート、ユーザーID設定
- **有効化に必要な手動作業（後日）**: Firebaseプロジェクト作成 → `GoogleService-Info.plist`/`google-services.json` 配置 → `@react-native-firebase/app`+`analytics` インストール → app.jsonにプラグイン追加 → `lib/analytics.ts` の該当行を差し替え → EAS開発ビルド。`lib/analytics.ts` 冒頭に手順をコメント記載済み

### 指示_25 アプリ内フィードバック ✅（**SQL必要 → 0035**）
- `supabase/migrations/0035_app_feedback.sql` 新規（要実行）
- `app/feedback.tsx` 新規（カテゴリ選択 + メッセージ + デバイス情報/バージョン自動記録 + 送信）
- `lib/rating.ts` 新規（取引5/15/50回でストアレビュー促進。`expo-store-review` インストール済み）
- 取引保存成功後に `checkRatingPrompt()` を呼び出し
- 設定に「フィードバック」導線追加 + 翻訳 `feedback.*`(4言語)
- ※ ストアレビューのネイティブ動作には開発ビルドが必要（Expo Goでは限定的）

### 指示_26 パフォーマンス最適化 ✅（**SQL必要 → 0036**）
- `components/feed-card.tsx` の `FeedCard` を `React.memo` 化
- `lib/image.ts` 新規（投稿1200px/アバター400px圧縮、`expo-image-manipulator` インストール済み）。`upload-image.ts`・`upload-media.ts` のアップロード前に圧縮を適用
- `app/_layout.tsx` で `SplashScreen` を手動制御（初期化完了まで表示維持）
- `supabase/migrations/0036_performance_indexes.sql` 新規（要実行・任意）
- ※ FlatList最適化props・expo-imageキャッシュは既に導入済みだった

### 指示_27 全言語翻訳チェック ✅（SQL不要）
- `scripts/check-translations.js` 新規（4言語の葉キー不一致を検出。`npm run check:i18n`）
- 実行結果: **全4言語とも662キーで完全一致**（監査時の不一致は本作業で解消済み）

### 指示_28 ストア掲載準備 ✅（SQL不要）
- `docs/app-store-metadata.md` を更新:
  - 中国語の誤記をすべて修正（4言語に統一）
  - スペイン語・ポルトガル語のストア説明文を追加
  - ES/PT のASOキーワード追加
  - Google Play 固有セクション追加（フィーチャーグラフィック1024×500、Data Safety、短い説明）
  - ユーザー手動作業リスト追加

---

## 検証状況
- `npx tsc --noEmit`: **エラー0**
- `npm run check:i18n`: **4言語一致**
- `npx expo lint`: **エラー0**（警告33件は本作業前から存在する既存のもの。増減なし）
- ⚠️ **実機/シミュレーターでのUI動作確認は未実施**（Expo実行環境がないため）。お手元での確認を推奨

## 追加インストールしたパッケージ
- `expo-store-review`（指示_25）
- `expo-image-manipulator`（指示_26）

## まだコミットしていません
変更は作業ツリーにあります。内容を確認後、コミットしたい場合は声をかけてください。

## 後日のネイティブ作業まとめ（コード不可）
- 指示_24: Firebase本体の設定ファイル + プラグイン + 再ビルド
- 指示_25: ストアレビューは開発ビルドで動作確認
- 指示_28: スクショ/フィーチャーグラフィック作成、ストア登録、EAS Build/Submit
