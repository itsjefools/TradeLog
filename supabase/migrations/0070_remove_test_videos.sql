-- 0070 旧テスト動画の削除
-- ============================================================================
-- 0029 等で投入された初期テスト動画3本を削除し、0069 のキュレーション済みカタログに統一する。
--   ・FX初心者が最初に知るべきこと  (SAMPLE_VIDEO_ID_1)
--   ・ローソク足の基本パターン        (SAMPLE_VIDEO_ID_2)
--   ・移動平均線の使い方              (SAMPLE_VIDEO_ID_3) ← is_free=false + youtube の不整合行
--
-- 目印 'SAMPLE_VIDEO_ID_%' で限定するので、0069 の本番動画(YT_REPLACE_/CF_REPLACE_)や
-- 将来の実動画を巻き込まない。冪等（既に削除済みなら 0 件）。
-- ============================================================================

delete from public.school_videos
where youtube_video_id like 'SAMPLE_VIDEO_ID_%';
